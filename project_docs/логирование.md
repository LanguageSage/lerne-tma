Searched for "logging."
Viewed main.py:15-40

Да, логирование в проекте реализовано на нескольких уровнях:

---

### 1. Бэкенд-логирование (Python / FastAPI)
* **Стандартный модуль `logging`**: В главной точке входа [main.py:L25](file:///c:/121/Lerne_projekt/tma/api/main.py#L25) настроен системный логгер уровня `INFO`.
* **Логгеры по модулям**: Во всех роутерах и сервисах ([ai_service.py](file:///c:/121/Lerne_projekt/tma/api/ai_service.py), [ai_clients.py](file:///c:/121/Lerne_projekt/tma/api/ai_clients.py), [media.py](file:///c:/121/Lerne_projekt/tma/api/services/media.py), [cards.py](file:///c:/121/Lerne_projekt/tma/api/services/cards.py)) созданы собственные экземпляры `logger = logging.getLogger(__name__)`.
* **Что фиксируется в логах**:
  - Все вызовы ИИ-провайдеров: выбор модели, ошибки 503/429, задержки ответа и повторные попытки (retries).
  - Сборка и загрузка TTS-аудиофайлов через edge-tts в Supabase Storage.
  - Транзакции и ошибки Peewee ORM в БД Supabase PostgreSQL.
  - События аутентификации Telegram / Android и рефреш сессий.

---

### 2. Логирование бизнес-событий в БД (Audit Log & SRS)
* **`TMAReviewHistory`** ([models.py](file:///c:/121/Lerne_projekt/tma/api/models.py)): лог истории обучения карточек (оценка SRS, старый/новый интервал, время отклика).
* **`TMAOfflineBatch`**: журнал транзакций офлайн-синхронизации между клиентом и сервером.

---

### 3. Клиентское логирование (Frontend / React)
* **Консольные логи (`console.log`, `console.error`)**: фиксируют статусы работы Dexie.js (IndexedDB), переключение на офлайн-режим [api.js:L188](file:///c:/121/Lerne_projekt/tma/app/src/services/api.js#L188) и запуск фоновой синхронизации в `syncService.js`.
* **Пользовательские ошибки**: всплывающие тосты (`showToast`) для отображения критических сбоев сети и ответов ИИ.